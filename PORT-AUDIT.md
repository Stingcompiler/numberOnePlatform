# PORT-AUDIT — نقل تطبيق سطح المكتب إلى Tauri v2

**المرحلة ٠ — تدقيق فقط.** لم يُكتب أي كود ولم يُعدَّل أي ملف. هذه الوثيقة هي المخرج الوحيد.

**تاريخ التدقيق:** ٢٠٢٦-٠٩-٠٦ · **الفرع:** `desktop-player-hardening` · **آخر commit:** `15448fc`

---

## ٠ — التحقق من مصادر الحقيقة

البرومبت طلب مني التحقق بنفسي قبل المتابعة. فحصت الثلاثة، والنتيجة تؤكد ما وُصف:

| المجلد | ما هو فعلًا | دوره في النقل |
|---|---|---|
| `desktop/` | تطبيق MAUI — تطبيق الطالب لسطح المكتب، ١٥ شاشة | **المرجع الأول** للسلوك والشكل |
| `mobile/` | React Native / Expo Router — تطبيق الطالب للهاتف، ١٨ مسارًا | مرجع للأسلوب في React |
| `frontend/` | React — **لوحة إدارة**: ٤٣ صفحة تحت `dashboard/` | ليس مصدرًا للواجهة |

الدليل على أن `frontend/` ليست تطبيق طالب:

- صفحاتها: `backups` · `course-access` · `password-management` · `exchange-rates` · `lecture-supervisors` · `exams/create` · `registration-conditions` · `finance` · `announcements`
- وفي `pages/public/` توجد صفحة اسمها **`StudentAppOnlyPage.jsx`** — الويب نفسه يحيل الطالب إلى التطبيق
- عدد الصفحات: `dashboard/` = ٤٣ · `public/` = ٧

**الخلاصة:** لا توجد شاشة طالب واحدة في `frontend/` يمكن نقلها. تُستخدم فقط لفهم عقد الـ API ولصفحة الدخول العامة.

---

## ١ — جرد MAUI

### الشاشات (١٥)

| الشاشة | الملف | الوظيفة |
|---|---|---|
| الدخول | `LoginPage.xaml` | مستخدم/كلمة مرور، إظهار كلمة المرور، عرض معرّف الجهاز ونسخه، تأكيد ربط الجهاز |
| الحظر | `BlockedPage.xaml` | حالتان: الحساب مرتبط بجهاز آخر / الجهاز مرتبط بطالب آخر. تعرض هاتف الإدارة |
| القشرة | `ShellPage.xaml` | الشريط الجانبي، الشريط العلوي، البحث، جرس الإشعارات، مبدّل السمة، تسجيل الخروج |
| الرئيسية | `HomeView.xaml` | ٤ بطاقات إحصاء، بطاقات وصول سريع (٧)، لافتة بث، اختبارات متاحة، تابع من حيث توقفت |
| الكورسات | `CoursesView.xaml` | جدول الكورسات مع بحث وتقدّم |
| تفاصيل الكورس | `CourseDetailView.xaml` | الوحدات والمحاضرات، شريط تقدّم |
| المحاضرات | `LecturesView.xaml` | قائمة مسطّحة لكل المحاضرات + مرشّح «لم تُشاهَد بعد» |
| المحاضرة | `LessonView.xaml` | المشغّل، العلامة المائية، رصيف الوحدة، زر الإكمال، التمرين |
| البث المباشر | `LiveView.xaml` | الجلسات، فتح رابط خارجي |
| الاختبارات | `ExamsView.xaml` | جدول الاختبارات المتاحة |
| خوض الاختبار | `ExamRunnerView.xaml` | مؤقّت، أسئلة، تسليم مع تأكيد، تسليم تلقائي عند انتهاء الوقت |
| النتائج | `ResultsView.xaml` | جدول مع ترقيم صفحات وفرز |
| تفاصيل المحاولة | `AttemptDetailView.xaml` | إجابات المحاولة |
| الإشعارات | `NotificationsView.xaml` | القائمة، تعليم كمقروء، تعليم الكل |
| الملف الشخصي | `ProfileView.xaml` | البيانات الشخصية، الجهاز المرتبط، الملف المالي |

### طبقة المنطق (`NumberOne.Core`)

- **١٩ view model** — كلها بلا اعتماد على MAUI، قابلة للاختبار بلا واجهة
- **`SectionState<T>`** — حالة لكل منطقة: تحميل / بيانات / فراغ / خطأ. كل شاشة تتكوّن من مناطق مستقلة
- **`ApiAttempt`** — غلاف لا يرمي استثناءً لمسارات الكتابة
- **`UiText`** — الأرقام العربية الهندية، تصريف العدد بأشكاله الأربعة، تنسيق التواريخ

### الخدمات

`StudentApi` · `AuthService` · `AuthenticatingHandler` · `RefreshEndpoint` · `SecureTokenStore` · `SiteContactService` · `PlayerNavigationPolicy` · `DrfError` · `ApiClientFactory`

### الـ endpoints (١٥)

```
auth/login/          auth/logout/        auth/me/         auth/refresh/
academic/my-courses/ academic/my-progress/ academic/my-submissions/ academic/submit/
exams/student/list/  live/my-sessions/
notifications/       notifications/count/ notifications/read-all/ notifications/{id}/read/
public/site-data/
```

> **ملاحظة:** `auth/change-password/` **غير مستخدم عمدًا** ولا توجد أي واجهة لتغيير كلمة المرور. قيد قائم من الأصل.

### التخزين المحلي

| ما | أين | ملاحظة |
|---|---|---|
| التوكنات | `SecureStorage` (DPAPI على ويندوز، Keychain على ماك) | قراءات مُسلسلة ومحفوظة في الذاكرة |
| السمة | `Preferences` مفتاح `app_theme` | الفاتح افتراضيًا |
| سجل الأعطال | `FileSystem.AppDataDirectory/crash.log` | كتم تكرار، سقف ٥١٢ ك.ب |
| الحافظة | `Clipboard.Default` | نسخ معرّف الجهاز فقط |

### القوائم والاختصارات وحوارات النظام

- **لا توجد قائمة تطبيق ولا شريط قوائم** — لا `MenuBarItem` ولا `KeyboardAccelerator` في أي مكان
- **لا اختصارات على مستوى التطبيق.** الاختصارات الوحيدة داخل المشغّل نفسه (JS): مسافة، أسهم، `M`
- **حوارات النظام:** ثلاثة فقط — رسالة العطل، تأكيد تسليم الاختبار، تعذّر فتح الصفحة
- **`Launcher.OpenAsync`** في ٣ مواضع لفتح روابط خارجية (جلسات البث، روابط الموقع)

---

## ٢ — جرد mobile (Expo Router)

```
(tabs)/  home · courses · exams · results · notifications · profile
         courses/[id] · courses/lesson/[lessonId]
         exams/[id] · exams/attempt
         notifications/index · notifications/[id]
         results/[id] · live-podcast · login · index
```

مكوّنات مشتركة في `src/components/`: `Button` · `Card` · `Input` · `StatusView` · `ThemeToggle` · `CourseCard` · `ExamCard` · `EmptyState`

هذه المكوّنات هي أقرب نقطة انطلاق لكتابة نفس الشاشات في TypeScript.

---

## ٣ — مصفوفة الشاشات

| الشاشة | MAUI | mobile | الفرق |
|---|---|---|---|
| الدخول | ✓ | ✓ | MAUI تعرض معرّف الجهاز وتؤكّد الربط؛ راجع القسم ١٠ |
| الحظر | ✓ | ؟ | لم أجد مقابلًا صريحًا في الموبايل |
| الرئيسية | ✓ | ✓ | بطاقات الوصول السريع أُضيفت في MAUI من تصميم الموبايل |
| الكورسات | ✓ | ✓ | متطابقة |
| تفاصيل الكورس | ✓ | ✓ | متطابقة |
| المحاضرات (قائمة مسطّحة) | ✓ | ✗ | **موجودة في MAUI فقط** |
| المحاضرة | ✓ | ✓ | الحماية والعلامة المائية في MAUI أثقل |
| البث المباشر | ✓ | ✓ | متطابقة |
| الاختبارات | ✓ | ✓ | متطابقة |
| خوض الاختبار | ✓ | ✓ | متطابقة |
| النتائج | ✓ | ✓ | ترقيم وفرز في MAUI |
| تفاصيل المحاولة | ✓ | ✓ | متطابقة |
| الإشعارات | ✓ | ✓ | متطابقة |
| الملف الشخصي | ✓ | ✓ | الملف المالي والجهاز المرتبط في MAUI |

**غير متطابق ويحتاج قرارك:** صفحة «المحاضرات» المسطّحة، وشاشة الحظر.

---

## ٤ — الأسطح الأصلية (Native)

كل استدعاء أصلي في MAUI هو `user32.dll` فقط. لا COM، لا WMI، لا خدمات نظام أخرى.

| ما تفعله MAUI | الاستدعاء | المقابل في Tauri v2 | مدعوم؟ |
|---|---|---|---|
| منع التقاط الشاشة | `SetWindowDisplayAffinity(WDA_EXCLUDEFROMCAPTURE)` | `window.set_content_protected(true)` | ✅ نفس الاستدعاء تحت الغطاء |
| إلغاء عكس شريط العنوان | `Get/SetWindowLongW(GWL_EXSTYLE)` + `SetWindowPos` | **غير مطلوب** — علّة MAUI/WinUI لا تتكرر | ⚪ يسقط |
| كتابة عنوان النافذة بـ UTF-8 | `SetWindowTextA` ببايتات UTF-8 | **غير مطلوب** — Tauri يضبط العنوان بـ Unicode | ⚪ يسقط |
| فتح رابط خارجي | `Launcher.OpenAsync` | `tauri-plugin-opener` | ✅ |
| الحافظة | `Clipboard.Default` | `tauri-plugin-clipboard-manager` | ✅ |
| مخزن الأسرار | `SecureStorage` | `tauri-plugin-stronghold` أو keyring عبر Rust | ⚠️ يحتاج قرارًا — راجع ق١٠ |
| معرّف الجهاز (ويندوز) | `MachineGuid` من السجل `Registry64` | Rust: `winreg` | ✅ |
| معرّف الجهاز (ماك) | `IOPlatformUUID` عبر `ioreg` | Rust: `IOKit` أو نفس الاستدعاء | ✅ |
| فتح النافذة مكبّرة | `OverlappedPresenter.Maximize()` | `WindowBuilder::maximized(true)` | ✅ |
| إغلاق WebView2 قبل التفكيك | `WebView2.Close()` | يديره Tauri | ⚪ يسقط |

**ثلاثة أسطح تسقط بالكامل** لأنها علاجات لعلل MAUI/WinUI بعينها، لا متطلبات منتج.

---

## ٥ — طبقة حماية المحتوى (إلزامي)

### ٥-١ منع التقاط الشاشة

**ويندوز** — `WindowsWindowProtection.cs`:
```
IsSupported  =  OperatingSystem.IsWindowsVersionAtLeast(10, 0, 19041)
Protect      =  SetWindowDisplayAffinity(hwnd, WDA_EXCLUDEFROMCAPTURE)   // 0x11
```
يُطبَّق على النافذة الرئيسية عند `Created`. الكود يحذّر: التقارب لكل HWND، فأي نافذة منبثقة تحتاج استدعاءها بنفسها.

**ماك** — `MacWindowProtection.cs`، النص الحرفي:

> «لا يوجد مقابل لـ `WDA_EXCLUDEFROMCAPTURE` على macOS. `NSWindow.sharingType` مهجور وغير قابل للوصول من Mac Catalyst بأي طريقة مدعومة. فهذه تُرجع `false` ولا تفعل شيئًا، عمدًا: العلامة المائية هي الحماية الفعلية على macOS.»

```csharp
public bool IsSupported => false;
public bool Protect(object platformWindow) => false;
```

> ⚠️ **تطبيق الماك الحالي بلا حماية التقاط إطلاقًا.** الواجهة تقرأ `IsSupported` ولا تفترض وجود الطبقة.
>
> **في Tauri يمكن تجاوز ذلك:** Tauri تطبيق macOS أصلي لا Catalyst، و`set_content_protected` تضبط `NSWindow.sharingType = .none`. أي أن النقل يجعل ماك **أقوى** لا أضعف. هذا مكسب، ويجب توثيقه في `PORT-NOTES.md` لا تمريره بصمت.

### ٥-٢ العلامة المائية

الحماية الحقيقية على ماك، وطبقة ثانية على ويندوز.

- محتواها: **اسم الطالب** + **رقم هاتفه** + **طابع زمني** يتحدّث كل ثانية (`yyyy-MM-dd HH:mm:ss`)
- تنتقل بين **٤ زوايا** دوريًّا، حتى لا تُقصّ من التسجيل بقطع حافة واحدة
- **شقيقة للـ WebView لا محقونة فيه** — إطار YouTube عابر للأصل ولا يمكن الوصول إليه
- مرتبطة بإطار الفيديو لا بالنافذة، فتبقى فوق الصورة عند أي عرض
- غير تفاعلية فلا تعترض النقر

### ٥-٣ إغلاق الـ WebView (`SecurePlayerWebViewSetup.cs`)

ثلاث طبقات مرتّبة بحسب قوّتها:

**١. رفض التنقّل** — الطبقة التي تصمد:
- `NavigationStarting` → يُلغى كل ما ليس صفحة المشغّل
- `FrameNavigationStarting` → قائمة سماح بالمضيف والمسار
- `NewWindowRequested` → **يُرفض دائمًا** ولا يُفتح شيء مكانه
- `DownloadStarting` → يُلغى
- `PermissionRequested` → يُرفض (كاميرا، ميكروفون، موقع)
- `WindowCloseRequested` → يُتجاهل

**٢. تعطيل إمكانات المضيف:**
```
AreDefaultContextMenusEnabled     = false   // «فتح في تبويب جديد»
AreDevToolsEnabled                = false   // F12
AreBrowserAcceleratorKeysEnabled  = false   // Ctrl+N/T/O، Alt+Left
IsStatusBarEnabled                = false   // معاينة الرابط
IsSwipeNavigationEnabled          = false
IsZoomControlEnabled              = false
IsGeneralAutofillEnabled          = false
IsPasswordAutosaveEnabled         = false
```

**٣. السكربت المحقون** (`lesson-player.js`) — أضعف الطبقات ولا تُستخدم وحدها:
- يعيد بناء `head` و`body`
- **درع** فوق الإطار يبتلع كل نقرة
- أدوات تحكّم خاصة: تشغيل، تخطٍّ ±١٠ث، صوت، وقت، سرعة، ملء شاشة
- ستار معتم فوق الإطار حتى أول تشغيل، لأن YouTube ترسم شعارها و«Watch on YouTube» قبل البدء
- `window.open = () => null`
- إخفاء تلقائي بعد ٢٥٠٠ مللي ثانية

**قاعدتان مكتوبتان في رأس الملف ويجب نقلهما:**
1. الإعداد يُنفَّذ **مرة واحدة** لكل WebView — تكراره حقن نسختين من المشغّل
2. **كل معالج حدث يلتقط الاستثناء** — استثناء يهرب من معالج WebView2 يعود بلا رسالة ولا مكدّس

### ٥-٤ تشغيل الفيديو

```
الصفحة    :  {API_HOST}/academic/player/     ← صفحة على خادمنا
المشغّل    :  https://www.youtube.com/iframe_api
المعاملات  :  controls=0  disablekb=1  fs=0  rel=0  iv_load_policy=3  playsinline=1
السطح     :  920 × 518  =  ١١ بوصة بنسبة ١٦:٩ عند ٩٦ نقطة/بوصة
```

> ✅ **لا يوجد DRM في هذا التطبيق.** لا PlayReady ولا Widevine ولا EME في أي ملف. بحثت عنها صراحةً.
>
> قرار **WebView2 Evergreen** صحيح، لكن سببه أن Fixed Runtime يثبّت إصدارًا ويحرم التطبيق تحديثات الأمان — لا لأنه لا يدعم PlayReady.

**المتطلبات في Tauri:**
- **ويندوز:** نفس محرّك WebView2. إعدادات الإغلاق تحتاج الوصول إلى `ICoreWebView2` الخام عبر `with_webview()` و`webview2-com` — Tauri لا تكشفها مباشرة
- **ماك:** WKWebView — سطح API مختلف كليًّا، والمقابلات تحتاج تحقّقًا على جهاز

---

## ٦ — سلوكيات تُنقل حرفيًّا

كل بند هنا كلّف تشخيصًا، ويُفقد بصمت في أي نقل لا يقصده.

### معرّف الجهاز
- **ويندوز:** `MachineGuid` من `HKLM\SOFTWARE\Microsoft\Cryptography`، بـ `RegistryView.Registry64` **صراحةً** — عملية ٣٢-بت تُحوَّل إلى `Wow6432Node` وتقرأ معرّفًا **مختلفًا**
- **ماك:** `IOPlatformUUID` عبر `ioreg` — *غير مُتحقَّق منه على عتاد*
- يُشتق من العتاد **في كل مرة**، ويُجزّأ، و**لا يُخزَّن أبدًا**

### المصادقة والجلسة
- عند `401`: تجديد **مرة واحدة** ثم إعادة تشغيل الطلب
- توكنات التجديد **تدور وتُدرَج في القائمة السوداء** بعد الدوران — فطلبان متزامنان يُبطل أحدهما الآخر
- لذلك: `SemaphoreSlim` وطلب تجديد **واحد** مشترك بين كل الطلبات المتزامنة
- عند فشل التجديد: `SessionExpired` → مسح التوكنات → العودة لصفحة الدخول
- عند الإقلاع: استعادة الجلسة. `Unverified` (تعذّر سؤال الخادم) **تُعامل كنجاح** — إرسال الطالب لنموذج دخول لا يستطيع الوصول للخادم أيضًا يتركه عالقًا

### الاختبار
- مؤقّت تنازلي، و**تسليم تلقائي عند بلوغ الصفر**
- التسليم محمي بـ `ApiAttempt` وحارس `IsSubmitting` ولوحة إعادة محاولة — انقطاع اتصال لحظة انتهاء وقت الطالب كان يُنهي العملية

### النصوص العربية
- الأرقام **عربية هندية** في المحتوى
- **تصريف العدد بأشكاله الأربعة**: `لا توجد جلسات` · `جلسة واحدة` · `جلستان` · `٣ جلسات` · **و١١ فأكثر تعود للمفرد**: `١١ جلسة`
- محروسة باختبارات في `DesignPassTests`

### الخطوط
- Cairo (٣ أوزان) للواجهة، Tajawal (٣) للنص الجاري
- **محزومة لا مجلوبة من الشبكة** — أجهزة المدارس خلف مرشّحات، وخط ويب يفشل يسقط إلى محرف لاتيني لا يشكّل العربية
- كل وزن يُسجَّل كعائلة مستقلة (MAUI لا تصل إلى ٥٠٠ عبر `FontAttributes`)
- ⚠️ `OpenSans` ما زال محزومًا وغير مستخدم بعد آخر تغيير — لا تنقله

### الأعطال
- **القاعدة: المكدّس لا نوع الاستثناء.** عطل بلا إطار مُدار = من تحت كودنا → يُسجَّل ولا يُعرض
- عطل يحمل إطارًا من كودنا → يُعرض
- سقف السجل ٥١٢ ك.ب، وكتم تكرار (٣ لكل توقيع)

### تفاصيل واجهة صغيرة لكنها مقصودة
- **الحقول الاختيارية الفارغة** في الملف الشخصي تعرض شرطة `—` لا فراغًا
- **الرصيد**: أصفر تحذيري **فقط** عند وجود مستحق؛ مسدَّد = حبر عادي. يُقرأ بثقافة ثابتة
- **السمة الفاتحة افتراضيًا** — تُضبط قبل أول نافذة، لأن الدخول والشاشات المحظورة تظهر قبل بناء القشرة
- **ملء الشاشة لا يغيّر أبعاد الصورة** — يخفي ما حولها فقط
- **الأيقونات بلا `Fill`** — Lucide خطوط مفتوحة، والتعبئة تدخل مسار قصٍّ يرفضه Direct2D
- **كل أيقونة تملك شكلها الهندسي** — المشاركة تنقلها بين العناصر

### المثبّت
- **لكل مستخدم، بلا رفع صلاحيات إطلاقًا** — الطالب على جهاز المدرسة بلا صلاحيات مدير
- عربي بالكامل مع `RightToLeft`
- ذاتي الاحتواء — لا يُطلب من مدرسة تثبيت وقت تشغيل أولًا
- الإزالة لا تمسح التوكنات ولا سجل الأعطال

---

## ٧ — Design tokens

مستخرجة من `desktop/NumberOne.Desktop/Resources/Styles/Tokens.xaml` — **لا من `frontend/`**.

### الألوان (فاتح / داكن)

| الرمز | فاتح | داكن |
|---|---|---|
| الخلفية | `#F8FAFF` | `#0D1117` |
| السطح | `#FFFFFF` | `#131A27` |
| السطح المرتفع | `#FFFFFF` | `#1A2235` |
| الشريط الجانبي | `#FFFFFF` | `#131A27` |
| الحدود | `#E2E8F0` | `#243044` |
| الحدود القوية | `#CBD5E1` | `#2E3D55` |
| النص | `#0F172A` | `#E6EDF7` |
| النص الثانوي | `#475569` | `#9BA9C2` |
| النص الخافت | `#64748B` | `#7E8DA8` |
| التمرير | `#F1F5F9` | `#1A2235` |
| **الأساسي** | `#C0392B` | `#E74C3C` |
| الأساسي عند التمرير | `#A93226` | `#EC6152` |
| **اللهجة** | `#1A56DB` | `#4C8DFF` |
| اللهجة عند التمرير | `#1544B0` | `#69A0FF` |
| النجاح | `#059669` | `#22C55E` |
| التحذير | `#D97706` | `#F59E0B` |

وألوان شفافة للتظليل: `ActiveLight #141A56DB` · `PrimaryTintLight #14C0392B` · `SecondaryTintLight #141A56DB` · `WarningTintLight #14D97706`

### الطباعة

```
FontTitle 20 · FontHeading 15 · FontBody 13 · FontSecondary 12 · FontLabel 11
واجهة : Cairo / CairoMedium / CairoBold
نص    : Tajawal / TajawalMedium / TajawalBold
```

### الهندسة

```
RadiusPanel 8 · RadiusControl 6 · CardPadding 16
سطح الفيديو 920×518 · الشريط الجانبي 200 · أزرار الأيقونات 28×28 · الأيقونات 16 (صغيرة 13)
```

### RTL
- الاتجاه على `NavigationPage` لا على النافذة — ضبطه على النافذة يعكس شريط العنوان
- الـ WebView يُجبر على `LeftToRight` — وإلا انقلبت الصورة
- أرقام معيّنة تبقى LTR: الهاتف، اسم المستخدم، معرّف الجهاز، التواريخ، الوقت

---

## ٨ — نطاقات CSP

مستخرجة من الكود لا من قائمة عامة:

| النطاق | لماذا | المصدر |
|---|---|---|
| `https://numberoneschools.com` | الـ API وصفحة المشغّل | `ApiEndpoints.DefaultBaseUrl` |
| `https://www.youtube.com` | `iframe_api` + الإطار | `lesson-player.js:288` |
| `https://youtube.com` | إعادة توجيه | `PlayerNavigationPolicy.FrameHosts` |
| `https://www.youtube-nocookie.com` | بديل مسموح | `PlayerNavigationPolicy.FrameHosts` |
| `https://youtube-nocookie.com` | بديل مسموح | `PlayerNavigationPolicy.FrameHosts` |

> ⚠️ القائمة أعلاه من التنقّل المسموح به فقط. YouTube تجلب وقت التشغيل من `s.ytimg.com` و`i.ytimg.com` و`*.googlevideo.com` — **هذه لم أتحقق منها من الكود** لأنها تُطلب داخل الإطار. يجب رصدها من جلسة تشغيل حقيقية قبل تثبيت الـ CSP، وإلا توقّف الفيديو بصمت.

---

## ٩ — العوائق

| # | العائق | السبب |
|---|---|---|
| ١ | **معرّف جهاز ماك غير مُتحقَّق منه** | الكود نفسه يقول `UNVERIFIED ON HARDWARE`. النقل يرث افتراضًا غير مختبر، وخطؤه يعني ربط جهاز خاطئًا |
| ٢ | **إعدادات إغلاق WebView2 غير مكشوفة في Tauri** | تحتاج `ICoreWebView2` الخام عبر `with_webview()` — عمل Rust interop حقيقي، وأي إعداد يفشل يفتح بابًا |
| ٣ | **مقابلات WKWebView على ماك مجهولة** | سطح API مختلف. لا أستطيع تأكيد وجود مقابل لكل بند في ٥-٣ دون جهاز ماك |
| ٤ | **مخزن الأسرار في Tauri** | لا مقابل مباشر لـ `SecureStorage`. `stronghold` ملف مشفّر لا Keychain/DPAPI — تغيير في نموذج التهديد |
| ٥ | **نطاقات YouTube وقت التشغيل** | راجع القسم ٨. لا تُستخرج من الكود |
| ٦ | **المثبّت العربي لكل مستخدم** | مُصدِّر Tauri يُنتج NSIS/MSI. مطابقة المثبّت الحالي (عربي كامل، RTL، بلا رفع صلاحيات) عمل قائم بذاته |
| ٧ | **التسليم التلقائي عند انتهاء الوقت** | يعتمد على مؤقّت يعمل والتطبيق مفتوح. أي فرق في سلوك المؤقّتات داخل WebView خامل يمسّ درجة طالب |
| ٨ | **شاشة الحظر غير موجودة في الموبايل** | لا مرجع React لها. تُنقل من MAUI وحدها |

---

## ١٠ — أسئلة مفتوحة

1. **صفحة «المحاضرات» المسطّحة** موجودة في MAUI وغائبة عن الموبايل. أنقلها؟
2. **مخزن الأسرار:** أقبل `stronghold` (ملف مشفّر) أم أكتب ربطًا بـ Keychain/DPAPI عبر Rust؟ الثاني يطابق نموذج التهديد الحالي ويكلّف أكثر.
3. ~~**ماك:** هل هناك جهاز للتحقق؟~~ — **مُجاب (٢٠٢٦-٠٩-٠٦):** الجهاز متوفّر، لكن **الأولوية لويندوز والماك لاحقًا**. أثره:
   - العائقان ١ و٣ ينتقلان من «مجهول» إلى «مؤجَّل» — يُسجَّلان في `BLOCKERS.md` ولا يوقفان التسليم
   - **لكن البنية يجب ألا تفترض ويندوز فقط.** إغلاق الـ WebView ومعرّف الجهاز ومخزن الأسرار: كلٌّ منها خلف واجهة (trait) بتنفيذ ويندوزي الآن وتنفيذ ماك لاحقًا. بلا هذا الفاصل يصير منفذ الماك إعادة كتابة لا إضافة
   - `set_content_protected` تعمل على المنصّتين، فلا فرق فيها
4. **حماية ماك ستصير أقوى** من MAUI. أُثبّتها أم أبقيها مطابقة للحالي؟ (توصيتي: أثبّتها)
5. **أسئلة الاختيار من متعدد بالمطابقة** تُعرض للقراءة فقط في MAUI — قيد معروف. أنقل القيد أم أُصلحه؟
6. **`OpenSans`** ما زال محزومًا وغير مستخدم. أُسقطه في النسخة الجديدة؟
7. **رسالة العطل** ما زالت تعرض نص الاستثناء للطالب — قيد مفتوح في MAUI. أنقله أم أكتب رسالة عامة؟
8. **انتهاء الجلسة** يعيد الطالب لصفحة الدخول بلا تفسير — الحوار المصمَّم لم يُبنَ قط. أبنيه في النسخة الجديدة أم أنقل السلوك كما هو؟

---

## توصية

النقل ممكن، وأصعب ما فيه ليس الشاشات بل **القسم ٥**. الشاشات منطق ظاهر وتُعاد كتابته بأمانة؛ أما طبقة الحماية فمتشابكة مع محرّك العرض، ونصفها لا يوجد له مقابل مكشوف في Tauri.

أقترح ألا نبدأ الدفعة ١ قبل إجابة السؤال ٢، لأن جوابه يغيّر البنية لا التفاصيل. السؤال ٣ أُجيب: ويندوز أولًا، وماك خلف واجهات.

### توصيتي في السؤال ٢ (مخزن الأسرار)

**اكتب ربطًا أصليًّا عبر Rust، لا `stronghold`.**

- `stronghold` ملف مشفّر، ومفتاحه يجب أن يعيش في مكان ما — على جهاز مدرسة مشترك هذا **أضعف** من الحالي، وتغيير في نموذج التهديد لا نقل له
- الحالي `SecureStorage` = DPAPI على ويندوز و Keychain على ماك: مفاتيح يديرها النظام ومربوطة بحساب المستخدم
- صندوق `keyring` في Rust يغطّي **المنصّتين بواجهة واحدة**: Credential Manager على ويندوز و Keychain على ماك
- أي أن «ويندوز الآن، ماك لاحقًا» **لا يكلّف شيئًا إضافيًّا** هنا — نفس الكود يعمل على الاثنين

الكلفة: ربط Rust صغير بدل مكوّن إضافي جاهز. المكسب: نموذج التهديد يبقى كما هو.
