# ما تُسمح به الواجهة، وسبب كل بند.
#
# TOML لا JSON، لأن البرومبت يطلب سبباً في تعليق لكل صلاحية، وJSON لا يحمل
# تعليقات. Tauri v2 يقرأ الصيغتين.
#
# القاعدة: لا يُضاف بند إلا عند وجود شاشة تحتاجه فعلاً. الدفعة ١ لا تحتاج
# شيئاً تقريباً — لا نظام ملفات، ولا شبكة من الواجهة، ولا حافظة، ولا فتح
# روابط. تُضاف مع الدفعات التي تحتاجها.

identifier = "default"
description = "أضيق مجموعة تكفي الدفعة ١: نافذة واحدة، وأمر واحد يقرأ حالة الحماية."
windows = ["main"]

permissions = [
  # إظهار النافذة بعد أن يجهز محتواها. بدونه تظهر النافذة بيضاء لحظة الإقلاع.
  "core:window:allow-show",

  # إغلاق النافذة من زر داخل الواجهة عند الحاجة لاحقاً (تسجيل الخروج).
  "core:window:allow-close",

  # قراءة السمة التي يفضّلها النظام. الافتراضي فاتح، لكن التبديل يحتاجها.
  "core:window:allow-theme",

  # الشبكة تمرّ عبر Rust، لا عبر fetch الخاص بالـ WebView.
  #
  # تُركت للـ WebView أولاً ليحكمها الـ CSP، ثم قِيس الواقع: الخادم لا يرسل
  # Access-Control-Allow-Origin، فيحجب المتصفح كل استجابة قبل قراءتها ولا يصل
  # شيء أصلاً. وتعديل الخادم خارج النطاق، ويعني فتح الـ API لأي أصل ويب من
  # أجل تطبيق سطح مكتب.
  #
  # والنطاق هنا أضيق من الـ CSP وحده: مضيف واحد مثبّت في الصلاحية نفسها.
  { identifier = "http:default", allow = [{ url = "https://numberoneschools.com/*" }] },
]

# التوكنات تُقرأ وتُكتب بأوامر يعرّفها التطبيق نفسه (tokens_get / tokens_save /
# tokens_clear)، وهي متاحة دون إدخالها هنا — فلا يصير مخزن الأسرار سطحاً عاماً
# تصل إليه أي صفحة عبر إضافة جاهزة.
