# تطبيق سطح المكتب — نسخة Tauri v2

نقل مستقل لتطبيق الطالب من .NET MAUI. المرجع الوحيد هو
[`../PORT-AUDIT.md`](../PORT-AUDIT.md).

تطبيق MAUI في `../desktop/` **يبقى يعمل كما هو**. لا شيء هنا يشير إليه ولا
يعدّله.

## البناء على هذا الجهاز — قيد مهم

Visual Studio Community المثبّتة هنا **ناقصة**: مجلد `VC\Tools\MSVC\*\lib`
فيها يحوي `onecore` فقط بلا `x64`، فلا يجد الرابط `msvcrt.lib`. وRust يختارها
لأنها الأحدث.

فالبناء يجب أن يمرّ عبر بيئة **Build Tools**:

```bat
"C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\VC\Auxiliary\Build\vcvars64.bat"
npm run tauri build
```

بدون ذلك يفشل الربط بـ `LNK1104: cannot open file 'msvcrt.lib'` — وهي رسالة
تبدو كأن Rust ناقص بينما العيب في تثبيت Visual Studio.

## فخّ ثانٍ: البناء القديم

أصول الواجهة **تُدمج داخل الملف التنفيذي وقت الترجمة**. فبعد `npm run build`
وحده يبقى الملف التنفيذي حاملًا واجهةً قديمة، ويبدو التعديل كأنه لم يعمل بينما
هو ببساطة غير موجود في ما شُغِّل.

بعد كل تعديل في `src/` يجب إعادة بناء الجانب الأصلي أيضًا:

```bash
npm run build
cargo build --manifest-path src-tauri/Cargo.toml   # داخل بيئة vcvars64
```

هذه هي المشكلة نفسها التي شحنت نسخة قديمة من الـ installer في تطبيق MAUI —
وقارنُ الطوابع الزمنية بين `dist/` والملف التنفيذي يكشفها في ثانية.

## التشغيل للتطوير

```bash
npm install
npm run tauri dev
```

## ما تحتاجه المنصّة

- **ويندوز 10 نسخة 2004 (build 19041)** فما فوق — الحماية تحتاج
  `WDA_EXCLUDEFROMCAPTURE` وهي من هذه النسخة
- **WebView2 Evergreen Runtime** حصرًا. لا Fixed Runtime: يثبّت إصدارًا
  ويحرم التطبيق تحديثات الأمان
- **ماك لاحقًا.** البنية تفصل الأسطح الأصلية في وحدات مستقلة تحسّبًا لذلك

## البنية

```
src/            الواجهة — React + TypeScript + Tailwind
src-tauri/
  src/
    main.rs        نقطة الدخول
    lib.rs         التركيب، والفاصل بين المنصّات
    protection.rs  حماية المحتوى، مع تحقّق من النظام لا افتراض
  capabilities/    الصلاحيات، بصيغة TOML لتحمل تعليقًا لكل بند
  tauri.conf.json  النافذة و CSP والحزمة
```

## ما أُنجز

| الدفعة | المحتوى |
| --- | --- |
| ١ | الهيكل، ونافذة تعمل، وحماية محتوى **متحقَّق منها** |
| ٢ | المصادقة، والجلسة، وربط الجهاز |
| ٣ | الإطار، والمسارات الثمانية، و RTL |
| ٤أ | الكورسات · تفاصيل الكورس · نمط الأقسام |
| ٤ب | الرئيسية · المحاضرات |
| ٤ج | الاختبارات · خوض الاختبار · النتائج · تفاصيل المحاولة |
| ٤د | الإشعارات · الملف الشخصي · البث المباشر · شاشتا الحظر |
| ٥ | المحاضرة · المشغّل · العلامة المائية · إغلاق الصفحة |
| ٦ | انتهاء الجلسة · شريط انقطاع الشبكة · نبض الشارة · موضع التنبيه |
| ٧ | الحزمة والمثبّت — راجع `PACKAGING.md` |

الباقي — **شاشة الدخول الحقيقية** والتوقيع — لم يُنقل بعد.

> **لا يُوزَّع المثبّت على الطلاب بعد.** شاشة الدخول فيه ما تزال أداة الفحص،
> وتكتب للطالب حرفياً «أداة فحص — الشاشة الحقيقية في الدفعة ٤».

لا توجد قوائم ولا اختصارات لوحة مفاتيح في تطبيق MAUI، فلم تُضَف هنا: إضافتها
اختراعُ ميزة لا نقلُها.

## التحقّق

النافذة مستثناة من التقاط الشاشة، و WebView2 لا يكشف الـ DOM لأدوات الأتمتة،
فلا يمكن النظر إلى أي شاشة من خارج التطبيق. لذلك **يبلّغ التطبيق عن نفسه من
الداخل**: عند ضبط متغيّرات البيئة يسجّل ما رسمه فعلًا في
`%TEMP%\numberone-harness.log`.

```bat
set NUMBERONE_HARNESS_USER=...
set NUMBERONE_HARNESS_PASS=...
set NUMBERONE_HARNESS_ROUTE=home
src-tauri\target\debug\numberone-desktop.exe
```

`NUMBERONE_HARNESS_ROUTE` يقبل معرّف أي مسار، أو `kind:id` لفتح شاشة تفصيلية:
`course:4` أو `exam:12` أو `attempt:7` أو `lesson:135:4` (المحاضرة ثم كورسها،
لأن شريط الوحدة لا يُبنى بدونه)، و`blocked:account` أو `blocked:device`
لشاشتَي الحظر، و`probe` لتشغيل قياس بدل شاشة، و`chrome:offline` أو `chrome:expired`
لحالتَي الإطار اللتين لا تُبلغان بالنقر. وهذه هي الطريقة الوحيدة للوصول إلى شاشة لا مسار لها في القائمة
الجانبية.
كل هذا خلف `#![cfg(debug_assertions)]` ولا وجود له في بناء الإصدار.

الجلسة المستعادة تُحترم أيضًا: التشغيل الثاني لا يمرّ بشاشة الدخول، وكان يهبط
على المسار الافتراضي ويبلّغ عن لا شيء.
