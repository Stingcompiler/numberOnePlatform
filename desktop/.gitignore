# .NET build output
bin/
obj/
*.user
*.suo
.vs/

# Publish output
publish/
*.appx
*.msix

# Built installers. The payload is ~240MB published and the installer
# itself around half that; neither belongs in the history.
installer/out/
